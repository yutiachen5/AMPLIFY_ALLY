dir: outputs
resume: False
resume_it: null # round to resume from when resume=True; required if resume=True
eval_steps: 100
pg_eval_steps: 1000
max_checkpoints: 100
gradient_clipping: 1
gradient_accumulation_steps: 1 
tf32: True
disable_tqdm: False